#include<stdio.h>/*
_=''''
cpr() {
    f=$1
    shift
    f=$(eval 'printf %s "$'"$f"'"')
    i=0
    l=$(printf %s "$f" | wc -c)
    while [ $i -lt $l ]; do
        c=$(printf %s "$f" | cut -c $((i+1)))
        if [ "$c" = "%" ]; then
            i=$((i+1))
            s=$(printf %s "$f" | cut -c $((i+1)))
            case "$s" in
                s)a=$1;shift;printf %s "$a" ;;
                c)a=$1;shift
                    case $a in
                        ''|*[!0-9]*) printf %s "$a" ;;
                        *) printf "\\$(printf '%03o' "$a")" ;;
                    esac
                    ;;
                %) printf %s "%" ;;   # handle %%
            esac
        else
            printf %s "$c"
        fi
        i=$((i+1))
    done
}

a(){ cpr s 10 10 36 36 36 10 10 10 10 10 34 "$s" 34 10 10 10;exit 0;};_=<<"#define a"
''';import sys;a,q,d=10,34,36
def b():print(s%(a,a,d,d,d,a,a,a,a,a,q,s,q,a,a,a),end='');sys.exit(0);'''
#!perl
sub a{10}sub k{34}sub d{36}sub b{printf($s,a,a,d,d,d,a,a,a,a,a,k,$s,k,a,a,a);exit 0;}$
#'''#*/
#define a
#define s char*z
#define b()int main(void){enum{q=34,d=36,b=92,n=10};printf(z,n,n,d,d,d,n,n,n,n,n,q,z,q,n,n,n);}
s="def b():print(s%%(a,a,d,d,d,a,a,a,a,a,q,s,q,a,a,a),end='');sys.exit(0);'''%c#!perl%csub a{10}sub k{34}sub d{36}sub b{printf(%cs,a,d,d,d,a,a,a,a,a,k,%cs,k,a,a,a);exit 0;}%c%c#'''#*/%c#define a%c#define s char*z%c#define b()int main(void){enum{q=34,d=36,b=92,n=10};printf(z,n,n,d,d,d,n,n,n,n,n,q,z,q,n,n,n);}%cs=%c%s%c;%ca%cb()%c";
a
b()
